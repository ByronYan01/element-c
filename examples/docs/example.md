## Button 按钮

常用的操作按钮。

:::demo 使用`type`、`plain`、`round`和`circle`属性来定义 Button 的样式。

```html
<template>
  <el-button type="primary">主要按钮</el-button>
</template>

<script>
  export default {
    data() {
      return {}
    },
  }
</script>
```

:::
